# ETN 启动器实施指南 v1.0

目标： 帮助从 0 开始构建一个可运行的：

- 启动器
- 热更新系统
- 版本校验系统

强调： 先做“能跑的最小系统”，再逐步增强。

---

# 一、先确定组件边界（不要一上来就写代码）

系统分为 3 个模块：

## 1. launcher.exe（启动器）

负责：

- 检查版本
- 下载补丁
- 校验文件
- 决定是否允许启动

不负责：

- 游戏逻辑
- 游戏资源加载

---

## 2. ETN.exe（游戏本体）

负责：

- 启动 Godot 引擎
- 加载主 PCK
- 加载补丁 PCK
- 运行游戏

不负责：

- 网络更新
- 版本比较
- 下载逻辑

---

## 3. 服务器（最简版）

负责：

- 提供 version.json
- 提供 patch\_xxx.pck 下载

第一阶段甚至可以：

- 用静态文件服务器
- 或对象存储

---

# 二、第一阶段目标：做出“可启动 + 可覆盖”的最小版本

## 步骤 1：先不做网络

本地准备：

```
ETN/
 ├─ ETN.exe
 ├─ ETN.pck
 ├─ patches/
 │    └─ patch_001.pck
```

修改一个场景文件：

- 在 patch\_001.pck 内放同路径资源

验证：

- 游戏是否加载补丁内容

如果成功：说明热更新机制成立。

---

## 步骤 2：实现补丁加载顺序

在游戏启动时：

1. 加载主 PCK
2. 遍历 patches 目录
3. 按编号排序
4. 依次加载

结果： 后加载覆盖前加载。

到这里，你已经拥有： 热更新能力。

---

# 三、第二阶段：加入版本控制

## 1. 创建本地 version.json

示例：

```
{
  "game_version": "1.0.0",
  "patch_level": 1
}
```

---

## 2. 启动器启动流程（本地模拟）

流程：

1. 读取 version.json
2. 读取“服务器版本文件”（可先放在本地文件模拟）
3. 比较 patch\_level
4. 若服务器更高：
   - 复制 patch 文件到 patches 目录
5. 启动 ETN.exe

---

# 四、第三阶段：加入文件完整性校验

## 1. 选择 Hash 算法

推荐：SHA256

---

## 2. 记录校验值

在 version.json 中加入：

```
{
  "patch_level": 3,
  "files": {
    "ETN.pck": "hash1",
    "patch_001.pck": "hash2"
  }
}
```

---

## 3. 启动前校验

启动器：

- 计算文件 Hash
- 与 version.json 对比
- 不一致 → 提示重新下载

到这里，你拥有：

- 防损坏
- 防误改
- 基础完整性保障

---

# 五、第四阶段：启动权限控制（可选）

目的： 防止用户绕过启动器直接运行游戏。

实现方式：

1. 启动器创建 session.tmp
2. 游戏启动时检测该文件
3. 若不存在 → 自动退出
4. 游戏启动成功后删除该文件

注意： 这是秩序控制，不是防破解。

---

# 六、开发顺序建议（极重要）

不要并行开发。

正确顺序：

1. 验证 PCK 覆盖
2. 实现补丁自动加载
3. 加入本地版本比较
4. 加入下载逻辑
5. 加入 Hash 校验
6. 最后优化 UI

很多人犯的错误是： 一开始就写完整更新系统。

正确做法是： 每一步都可运行。

---

# 七、组件职责最终清单

## 启动器承担：

- 网络请求
- 版本比较
- 下载补丁
- Hash 校验
- 写入文件
- 启动游戏

## 游戏承担：

- 加载资源
- 读取 patches
- 正常运行

## 服务器承担：

- 提供最新版本信息
- 提供补丁文件

---

# 八、第一周落地目标

第 1 天： 验证 PCK 覆盖

第 2–3 天： 实现补丁自动加载

第 4–5 天： 完成本地版本比较

第 6–7 天： 加入简单下载逻辑

一周后，你将拥有：

可热更新的独立游戏结构。

---

# 九、当前阶段不要做的事情

- 不要做加密
- 不要做复杂防破解
- 不要做多线程下载优化
- 不要做增量二进制补丁

这些都是 v2 以后的内容。

---

# 十、阶段性判断标准

当以下条件成立时，说明 v1 完成：

- 可以单独修改一个脚本并通过 PCK 覆盖
- 可以检测版本落后并下载补丁
- 可以校验文件完整性
- 游戏只能通过启动器正常运行

此时系统已经具备“发行级结构”。

之后才谈优化与高级功能。

